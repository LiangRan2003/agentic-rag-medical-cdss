# 测试报告

## 结果总览

| 指标 | 结果 | 含义 |
| --- | ---: | --- |
| 自动化回归测试通过率 | 100%（7/7） | 当前代码逻辑通过全部测试 |
| 检索节点场景通过率 | 100%（2/2） | 正常检索和空结果均符合预期 |
| 文档相关性路由通过率 | 100%（3/3） | 部分相关、全部相关、全部无关均正确路由 |
| 幻觉路由分支通过率 | 100%（2/2） | mock 返回 yes/no 时均进入预期分支 |
| 真实回答通过率 | 88.9%（8/9） | 真实 API 回答，经指南证据判分 |
| 真实幻觉回答率 | 11.1%（1/9） | 至少包含一条指南证据无法支持的声明 |
| Top-3 检索命中率 | 100%（9/9） | 便携式字符检索基线 |
| 应拒答成功率 | 100%（5/5） | 超出知识库范围时明确拒答 |

注意：幻觉路由分支通过率只证明控制流实现正确。真实质量指标来自 evaluation 下的独立在线评测；其判分由 LLM 按指南证据完成，适合工程回归，但不能替代临床专家审核。

## 覆盖范围

- 检索节点正确传递用户问题并返回文档。
- 检索器返回空结果时工作流保持稳定。
- 文档评分覆盖“部分相关”“全部相关”“全部不相关”三类路由。
- 无关文档会被过滤，并正确设置后续检索标记。
- 幻觉检查可区分有依据回答与无依据回答。
- 工作流图包含预期节点、普通边和条件路由。

## 成果价值

RAG 系统的关键不只是模型回答，还包括检索、过滤、生成和校验之间的控制流。测试固定了这些路由规则，使节点调整后出现的上下文丢失、误过滤和错误循环能够被及时发现。

## 真实质量评测标准

当前已运行 9 道来自仓库内五份指南的有依据题，以及 5 道应拒答题：

- 回答正确率 = 通过医学事实核对的回答数 / 总回答数。
- 幻觉率 = 含有至少一条指南无法支持的事实声明的回答数 / 总回答数。
- 检索命中率 = 前 3 个检索片段中包含标准依据的题目数 / 总题目数。
- 应拒答成功率 = 超出知识库范围时明确拒答且未编造的题目数 / 应拒答题目数。
- 引用可追溯率 = 关键结论能够定位到指南和原文片段的回答数 / 总回答数。

建议发布门槛：回答正确率不低于 90%，幻觉率不高于 5%，应拒答成功率不低于 95%。当前回答通过率和幻觉率尚未达到前两项门槛。

完整逐题结果见 evaluation/QUALITY_REPORT.md，原始结果见 evaluation/results.json，评测题及短证据见 evaluation/benchmark.json。

## 验证方式

    python -m pytest -q

测试结果：7 passed（工程逻辑测试，不代表真实医疗回答正确率）

在线质量评测需要在进程环境中提供 MEDICAL_API_KEY 和 EVALUATOR_API_KEY：

    python evaluation/evaluate_quality.py --reuse-benchmark

## 测试文件

- tests/conftest.py
- tests/test_workflow_nodes.py
- evaluation/evaluate_quality.py
- evaluation/QUALITY_REPORT.md
